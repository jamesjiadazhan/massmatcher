% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mz_match_clustering.R
\name{mz_match_clustering}
\alias{mz_match_clustering}
\title{Run m/z Matching and Adduct/Isotope Clustering}
\usage{
mz_match_clustering(
  met_raw_wide,
  metabolite_database = NULL,
  database = c("metorigindb", "pubchem"),
  hmdb_concentration = NULL,
  mz_threshold = 10,
  biospecimen = "Blood",
  All_Adduct = NULL,
  ion_mode = "positive",
  adduct_correlation_r_threshold = 0.39,
  adduct_correlation_time_threshold = 6,
  isotopic_correlation_r_threshold = 0.71,
  isotopic_correlation_time_threshold = 4,
  imputation_method = "half_min",
  output_dir = ".",
  write_output = TRUE,
  show_progress = TRUE,
  progress_callback = NULL
)
}
\arguments{
\item{met_raw_wide}{A feature table in wide format. The first column must be
`mz`, the second column must be `time`, and the remaining columns are sample
intensities.}

\item{metabolite_database}{Optional metabolite database with the columns
`Mono_mass`, `Most_abundant_isotopologue_mass`, `Name`, `Formula`,
`HMDB_ID`, `KEGG_ID`, `InChIKey`, `Subclass`, `has_NH3`, `has_H2O`,
`n_NH3`, and `n_H2O`. When `NULL`, the database specified by `database` is
loaded automatically.}

\item{database}{Database used when `metabolite_database` is `NULL`. Choose
either `"metorigindb"` or `"pubchem"`. Default is `"metorigindb"`.}

\item{hmdb_concentration}{Optional HMDB concentration table. If `NULL`, the
packaged HMDB concentration table is used via `hmdb_concentration_loading()`.}

\item{mz_threshold}{The ppm threshold used for m/z matching. Default is `10`.}

\item{biospecimen}{Biospecimen used to prioritize HMDB concentrations.
Default is `"Blood"`.}

\item{All_Adduct}{Adducts considered during m/z matching. When `NULL`, a
positive- or negative-mode default is selected from `ion_mode`.}

\item{ion_mode}{Ion mode string used in outputs. Default is `"positive"`.}

\item{adduct_correlation_r_threshold}{Spearman correlation threshold for
grouping adducts. Default is `0.39`.}

\item{adduct_correlation_time_threshold}{Retention-time window in seconds for
adduct correlation grouping. Default is `6`.}

\item{isotopic_correlation_r_threshold}{Spearman correlation threshold for
isotope pairing. Default is `0.71`.}

\item{isotopic_correlation_time_threshold}{Retention-time window in seconds
for isotope pairing. Default is `4`.}

\item{imputation_method}{Missing-value imputation method. One of
`"half_min"`, `"QRILC"`, or `NA`. Default is `"half_min"`.}

\item{output_dir}{Directory where CSV outputs are written. Default is `"."`.}

\item{write_output}{Logical; if `TRUE`, write the four CSV outputs. Default
is `TRUE`.}

\item{show_progress}{Logical; if `TRUE`, emit console progress messages and
a text progress bar. Default is `TRUE`.}

\item{progress_callback}{Optional function for external progress updates.
When provided, it is called as `progress_callback(step, total, detail)`.}
}
\value{
A named list containing the four result tables:
  `mz_only`, `mz_only_isotope`, `clustering_all`, and `clustering_main`.
}
\description{
`mz_match_clustering()` reuses the preprocessing, m/z matching, and
adduct/isotope clustering workflow inside the `massmatcher` package.
It intentionally omits class-file handling, retention-time mapping,
and precursor-product/transporter logic.

The function writes four CSV files by default:
`positive_metabolite_annotated_10ppm_mzonly.csv`,
`positive_metabolite_annotated_10ppm_mzonly_isotope.csv`,
`clustering_of_adduct_isotope_all_result.csv`, and
`clustering_of_adduct_isotope_main_result.csv`.
}
